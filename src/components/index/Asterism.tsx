/** Marks the bottom of a list — there's no closing border. aria-hidden. */
export function Asterism() {
  return (
    <div aria-hidden className="asterism pt-[var(--space-block)]">
      ⁂
    </div>
  );
}
